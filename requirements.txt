# این نرم‌افزار فقط با کتابخانهٔ استاندارد پایتون (tkinter) کار می‌کند و
# هیچ وابستگی اجباری ندارد.
#
# tkinter بخشی از پایتون است. اگر هنگام اجرا خطای «No module named tkinter»
# گرفتید، بستهٔ سیستمی آن را نصب کنید:
#   - Debian/Ubuntu:  sudo apt-get install python3-tk
#   - Fedora:         sudo dnf install python3-tkinter
#   - macOS/Windows:  معمولاً همراه پایتون رسمی نصب است.
#
# وابستگی‌ها برای کشیدن و رها کردن فایل:
tkinterdnd2>=0.3.0
windnd>=1.0.7; sys_platform == 'win32'
